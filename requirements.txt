# requirements.txt — SQL Server MCP Server
mcp[cli]>=1.9.0          # SDK oficial de MCP (incluye FastMCP)
pyodbc>=5.1.0            # Driver ODBC para SQL Server
pytest>=8.0.0            # Tests unitarios y de integración
pytest-mock>=3.14.0      # Mocking para tests
